import { findLast } from "index";
import { describe, it, expect } from "vitest";

describe("findLast tests", () => {
  describe("truthy", () => {
    it("", async () => {});
  });

  describe("falsy", () => {
    it("", async () => {});
  });
});
